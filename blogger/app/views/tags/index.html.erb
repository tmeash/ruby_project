<h1>Here are all available tags:</h1>
<ul>
	<% @tags.each do |tag| %>
		<li><%= link_to tag.name, tag_path(tag) %></li>
	<% end %>

<%= link_to "delete", tag_path(@tag), method: :delete, data: {confirm: "Really delete the tag?"} %>

</ul>